from PointManager import PointManager
from ShapeManager import ShapeManager
from Space import Space

def add_point(space):
    pass

def display_space_data(space):
    space.get_point_manager().list_points()
    space.get_shape_manager().list_polygons()